# conf/colors.zsh

#
# configure color support
#

autoload -U colors && colors

export LSCOLORS="Gxfxcxdxbxegedabagacad"
eval "$(dircolors -b $ZSH_CONF_DIR/dircolors)"

setopt auto_cd
setopt multios

# vim: set ft=zsh ts=2 sw=0 et:
